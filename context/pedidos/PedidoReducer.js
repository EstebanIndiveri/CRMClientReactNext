import{
    SELECCIONAR_CLIENTE,
    CANTIDAD_PRODUCTOS,
    SELECCIONAR_PRODUCTO
    }from'../../types';


export default(state,action)=>{
    switch (action.type) {
        case value:
            
            break;
    
        default:
            return state
    }
}